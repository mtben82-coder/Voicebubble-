name: iOS IPA Build

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build iOS IPA
    runs-on: macos-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true
      
      - name: Get Flutter dependencies
        run: flutter pub get
      
      - name: Run code generation
        run: flutter pub run build_runner build --delete-conflicting-outputs
      
      - name: Analyze code
        run: flutter analyze
        continue-on-error: true
      
      - name: Run tests
        run: flutter test
        continue-on-error: true
      
      - name: Install CocoaPods
        run: |
          cd ios
          pod install
      
      # Uncomment and configure these steps for signed builds
      # - name: Import Code-Signing Certificates
      #   uses: Apple-Actions/import-codesign-certs@v2
      #   with:
      #     p12-file-base64: ${{ secrets.IOS_CERTIFICATE_P12_BASE64 }}
      #     p12-password: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
      
      # - name: Download Provisioning Profiles
      #   run: |
      #     mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
      #     echo "${{ secrets.IOS_PROVISIONING_PROFILE_BASE64 }}" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision
      
      - name: Build iOS (Debug - No Codesign)
        run: |
          flutter build ios --debug --no-codesign
      
      # For unsigned release build (for testing purposes only)
      - name: Build iOS (Release - No Codesign)
        run: |
          flutter build ios --release --no-codesign
        continue-on-error: true
      
      # Uncomment for signed release build
      # - name: Build iOS (Release)
      #   run: |
      #     flutter build ios --release
      
      # - name: Build IPA
      #   run: |
      #     cd build/ios/iphoneos
      #     mkdir Payload
      #     cp -r Runner.app Payload
      #     zip -r voicebubble.ipa Payload
      
      # - name: Upload IPA
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: voicebubble-ios.ipa
      #     path: build/ios/iphoneos/voicebubble.ipa
      #     retention-days: 30
      
      # Uncomment to upload to TestFlight
      # - name: Upload to TestFlight
      #   if: github.ref == 'refs/heads/main'
      #   uses: apple-actions/upload-testflight-build@v1
      #   with:
      #     app-path: build/ios/iphoneos/voicebubble.ipa
      #     issuer-id: ${{ secrets.APPSTORE_ISSUER_ID }}
      #     api-key-id: ${{ secrets.APPSTORE_API_KEY_ID }}
      #     api-private-key: ${{ secrets.APPSTORE_API_PRIVATE_KEY }}
      
      - name: Build Info
        run: |
          echo "iOS build completed (unsigned for testing)"
          echo "To create a signed IPA for distribution:"
          echo "1. Configure code signing certificates in GitHub secrets"
          echo "2. Uncomment the signing steps in this workflow"
          echo "3. Ensure your Apple Developer account is properly configured"

